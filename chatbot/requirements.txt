# Cloud-friendly pins.
# - torch is intentionally NOT pinned: sentence-transformers will pull a wheel
#   that matches the host Python version. Hard-pinning torch causes
#   "no matching distribution" errors on Streamlit Cloud when wheel availability shifts.
# - Lower bounds keep us on known-good APIs; no upper bounds so pip can resolve.

pdfplumber>=0.11.0
sentence-transformers>=3.0.0,<4
chromadb>=0.5.0,<0.7
rank-bm25>=0.2.0
groq>=0.13.0
streamlit>=1.40.0
python-dotenv>=1.0.0
gdown>=5.0.0
numpy<2
