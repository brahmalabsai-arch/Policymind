# Force the CPU-only PyTorch index as the PRIMARY package source.
# This makes pip find torch (and its torch-* siblings) on the CPU index
# FIRST, before falling back to PyPI for everything else.
# Using --extra-index-url alone is not enough — pip still preferred
# PyPI's CUDA-bundled torch wheel.
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple/

torch
pdfplumber>=0.11.0
sentence-transformers>=3.0.0,<4
chromadb>=0.5.0,<0.7
rank-bm25>=0.2.0
groq>=0.13.0
streamlit>=1.40.0
python-dotenv>=1.0.0
gdown>=5.0.0
